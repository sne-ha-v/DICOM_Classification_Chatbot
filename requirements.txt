# Core web frameworks
streamlit>=1.28.0
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0

# Data science and ML
numpy>=1.24.0
# For macOS (Apple Silicon) use Apple's `tensorflow-macos` + `tensorflow-metal`.
# Keep `tensorflow-cpu` for non-mac platforms. Environment markers ensure the
# correct package is selected automatically.
tensorflow-macos>=2.13.0; sys_platform == "darwin"
tensorflow-metal>=0.8.0; sys_platform == "darwin"
tensorflow-cpu>=2.13.0; sys_platform != "darwin"
scikit-learn>=1.3.0
opencv-python-headless>=4.8.0

# Medical imaging
nibabel>=5.2.0
pydicom>=2.4.0
Pillow>=10.0.0

# AI and NLP
huggingface_hub>=0.17.0
requests>=2.31.0

# Configuration and utilities
python-dotenv>=1.0.0

# Legacy support (can be removed if not needed)
# openai>=1.0.0

python-multipart